{{#each blogposts as |blog| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{blog.name}} {{blog.user.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{blog.content}}</h3>
  </div>
</div>
{{/each}}
